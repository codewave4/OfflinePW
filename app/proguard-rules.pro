# ProGuard/R8 rules for OfflinePW.
#
# Note: minification is currently DISABLED for the release build
# (app/build.gradle -> minifyEnabled false), so this file is not active.
# Keep it ready in case R8/ProGuard gets enabled later.
#
# If you enable minification, add rules here as needed.
# This app does not use reflection, so no keep rules are required at the moment.
