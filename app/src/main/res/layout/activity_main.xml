<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/mainRootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/nordic_bg">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/nordic_bg"
        app:elevation="0dp">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:paddingHorizontal="14dp">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <!-- عنوان برنامه -->
                <TextView
                    android:id="@+id/tvAppTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="OfflinePW"
                    android:textColor="@color/nordic_text_primary"
                    android:textStyle="bold"
                    android:letterSpacing="0.08"
                    android:fontFamily="monospace"
                    android:textSize="15sp"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent" />

                <!-- دکمه‌های ۳ تایی کنترل بالا (؟ ، زبان ، تم) -->
                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent">

                    <!-- منوی بیشتر: گزارش سلامت رمزها + ترتیب لیست -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btnMore"
                        style="@style/Widget.Material3.Button.TonalButton"
                        android:layout_width="36dp"
                        android:layout_height="36dp"
                        android:insetTop="0dp"
                        android:insetBottom="0dp"
                        android:padding="0dp"
                        app:icon="@drawable/ic_more_vert"
                        app:iconSize="20dp"
                        app:iconPadding="0dp"
                        app:iconTint="@color/nordic_text_primary"
                        app:backgroundTint="@color/nordic_tonal_btn_bg"
                        app:strokeColor="@color/nordic_border"
                        app:strokeWidth="1dp"
                        app:cornerRadius="10dp"
                        android:layout_marginEnd="6dp"
                        android:contentDescription="More" />

                    <!-- دکمه درباره امنیت و حریم خصوصی (?) -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btnAbout"
                        style="@style/Widget.Material3.Button.TonalButton"
                        android:layout_width="36dp"
                        android:layout_height="36dp"
                        android:padding="0dp"
                        android:text="?"
                        android:textSize="13sp"
                        android:textStyle="bold"
                        android:fontFamily="monospace"
                        android:textColor="@color/nordic_text_primary"
                        app:backgroundTint="@color/nordic_tonal_btn_bg"
                        app:strokeColor="@color/nordic_border"
                        app:strokeWidth="1dp"
                        app:cornerRadius="10dp"
                        android:layout_marginEnd="6dp" />

                    <!-- دکمه تغییر زبان (FA / EN) -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btnLanguage"
                        style="@style/Widget.Material3.Button.TonalButton"
                        android:layout_width="wrap_content"
                        android:layout_height="36dp"
                        android:minWidth="44dp"
                        android:paddingHorizontal="10dp"
                        android:paddingVertical="0dp"
                        android:text="FA"
                        android:textSize="11sp"
                        android:textStyle="bold"
                        android:fontFamily="monospace"
                        android:textColor="@color/nordic_text_primary"
                        app:backgroundTint="@color/nordic_tonal_btn_bg"
                        app:strokeColor="@color/nordic_border"
                        app:strokeWidth="1dp"
                        app:cornerRadius="10dp"
                        android:layout_marginEnd="6dp" />

                    <!-- دکمه تغییر تم (DARK / LIGHT) -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btnThemeToggle"
                        style="@style/Widget.Material3.Button.TonalButton"
                        android:layout_width="wrap_content"
                        android:layout_height="36dp"
                        android:minWidth="46dp"
                        android:paddingHorizontal="8dp"
                        android:paddingVertical="0dp"
                        android:text="DARK"
                        android:textSize="11sp"
                        android:textStyle="bold"
                        android:fontFamily="monospace"
                        android:textColor="@color/nordic_text_primary"
                        app:backgroundTint="@color/nordic_tonal_btn_bg"
                        app:strokeColor="@color/nordic_border"
                        app:strokeWidth="1dp"
                        app:cornerRadius="10dp" />
                </LinearLayout>

            </androidx.constraintlayout.widget.ConstraintLayout>
        </androidx.appcompat.widget.Toolbar>

        <EditText
            android:id="@+id/etSearch"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:layout_marginHorizontal="12dp"
            android:layout_marginBottom="10dp"
            android:hint="جستجو در عنوان، حساب و تگ‌ها..."
            android:textColorHint="@color/nordic_text_secondary"
            android:textColor="@color/nordic_text_primary"
            android:textSize="13sp"
            android:paddingHorizontal="14dp"
            android:background="@drawable/bg_search_input" />
    </com.google.android.material.appbar.AppBarLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvVault"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingTop="4dp"
        android:paddingBottom="90dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <!-- دکمه‌ی بکاپ/بازیابی — بالای دکمه‌ی افزودن -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabBackup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="20dp"
        android:layout_marginBottom="88dp"
        app:fabCustomSize="48dp"
        app:maxImageSize="22dp"
        android:src="@drawable/ic_backup"
        app:backgroundTint="@color/nordic_tonal_btn_bg"
        app:tint="@color/nordic_text_primary"
        app:elevation="4dp" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAdd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="20dp"
        android:src="@drawable/ic_add"
        app:backgroundTint="@color/nordic_text_primary"
        app:tint="@color/nordic_bg"
        app:elevation="4dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
