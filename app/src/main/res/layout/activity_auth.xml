<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/nordic_bg"
    android:padding="24dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="12dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAuthHelp"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:text="?"
            android:textColor="@color/nordic_text_primary"
            android:textSize="14sp"
            android:textStyle="bold"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            app:backgroundTint="@color/nordic_tonal_btn_bg"
            app:cornerRadius="12dp"
            app:strokeColor="@color/nordic_border"
            app:strokeWidth="1dp" />

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAuthLang"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="wrap_content"
            android:layout_height="38dp"
            android:text="FA"
            android:textColor="@color/nordic_text_primary"
            android:textSize="12sp"
            android:textStyle="bold"
            app:backgroundTint="@color/nordic_tonal_btn_bg"
            app:cornerRadius="12dp"
            app:strokeColor="@color/nordic_border"
            app:strokeWidth="1dp" />
    </LinearLayout>

    <ImageView
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@mipmap/ic_launcher"
        android:layout_marginBottom="12dp" />

    <TextView
        android:id="@+id/tvAuthTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="OfflinePW"
        android:textColor="@color/nordic_text_primary"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_marginBottom="6dp" />

    <TextView
        android:id="@+id/tvAuthPrompt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enter your Master Password"
        android:textColor="@color/nordic_text_secondary"
        android:textSize="15sp"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <TextView
        android:id="@+id/tvAuthWarning"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your master password is never stored; therefore, if forgotten, recovery is mathematically impossible."
        android:textColor="#EF4444"
        android:textSize="12sp"
        android:gravity="center"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="24dp" />

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/tilMasterPassword"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:passwordToggleEnabled="true"
        app:passwordToggleTint="@color/nordic_text_secondary"
        app:boxStrokeColor="#3B82F6"
        app:hintTextColor="#3B82F6"
        android:hint="Master Password">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etMasterPassword"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textPassword"
            android:textColor="@color/nordic_text_primary"
            android:textSize="16sp"
            android:imeOptions="actionDone" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- فیلد ولت فریبنده — فقط در مرحله اولِ ساخت ولت دیده می‌شود (visibility در کد) -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/tilDecoyPassword"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:passwordToggleEnabled="true"
        app:passwordToggleTint="@color/nordic_text_secondary"
        app:boxStrokeColor="#F59E0B"
        app:hintTextColor="#F59E0B">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etDecoyPassword"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textPassword"
            android:textColor="@color/nordic_text_primary"
            android:textSize="16sp"
            android:imeOptions="actionDone" />
    </com.google.android.material.textfield.TextInputLayout>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnUnlock"
        style="@style/Widget.Material3.Button"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginTop="20dp"
        android:text="Unlock"
        android:textSize="15sp"
        android:textStyle="bold"
        android:textColor="@color/nordic_primary_btn_text"
        app:backgroundTint="@color/nordic_primary_btn_bg"
        app:cornerRadius="14dp" />

</LinearLayout>
